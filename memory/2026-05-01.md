# 2026-05-01

## Durable session memory
- JT7 phase remains **Cockpit + Runtime Hardening**; no phase jump yet, but the staging writeback trust path materially advanced.
- The JT7 scheduler now uses **single-pass-on-resume** catch-up semantics instead of silently skipping missed local schedule windows.
- `job-search-ui/scripts/run_jt7_chain.py` was hardened to call `gog` via absolute path with explicit `--account jonathon.thomason@gmail.com` and `--no-input`, plus improved stderr/stdout surfacing for future scheduler failures.
- The unresolved `2026-05-01 08:30` scheduler failure is still only narrowed to an opaque/transient `gog` exit-2 condition; it is not root-caused yet.
- Narrow audited staging→canonical writeback is now real: `apply_staging_writeback.py` can plan/apply a single staged promotion or merge, refresh local mirrors, and emit a runtime audit report.
- The local UI now has a thin runtime bridge (`local_api_server.mjs` + `runtimeApi.ts`) so staging `Promote` / `Merge` actions can hit the real runtime writeback path instead of only mutating local demo state.
- Live canonical `Jobs` rows created during proof work: `job_057` (Possible Finance), `job_058` (Upstart), `job_059` (Toast), and `job_062` (Affirm).
- A browser-driven proof of the staging promotion flow now exists via `job-search-ui/tests/staging-writeback.spec.ts`; the UI button path was validated against the live local API and live Sheets-backed writeback path.
- The browser proof exposed a real duplicate-protection bug: once a direct-board staged row was promoted into `Jobs`, planner duplicate detection still ignored it because of source/notes rules. `runtime/services/staging_writeback.py` was updated so promoted `Jobs` rows count as canonical duplicates for future writeback decisions.
- During duplicate-protection verification, `job_061` was intentionally archived and pointed back to canonical `job_060` to clean up duplicate tracker state.
- Local mirrors `job-search-ui/data_mirror/Jobs.json` and `Jobs.csv` were refreshed repeatedly and remain the recovery/audit mirror, while Google Sheets remains the canonical tracker.
- Drive persistence is currently snapshot-style and creates duplicate mirror copies instead of updating a single canonical mirrored file in place; this remains an open cleanup/system design issue.
- Important pushed commits from this workstream include: `2a09014` (persist staging writeback bridge and synced mirrors), `bc824d1` (mark JT7 idle and persist shutdown state), and `9fc2810` (harden staging writeback duplicate protection).
- Highest-leverage next step after this session: prove the **merge** branch through the browser/UI path, not just via planner/API evidence.
- Later the same night, that browser/UI merge proof was completed: Playwright was updated to start both the local API and a fresh preview build, duplicate candidates were surfaced more directly in the staging UI, and a targeted merge-only run passed against the live runtime path.
- The successful merge proof generated live runtime merge artifacts for `staged_job_083` → canonical `job_060`, confirming the browser click path can execute a real canonical merge through the runtime bridge.
- That same validation exposed a new create-path issue: browser-driven create writebacks do not yet reliably show the newly created canonical row in the in-app `Jobs` view after navigation, so create-path state-sync remains an open bug even though merge-path proof is now solid.
