## Pre-compaction durable memory
- Jonathon established a hard architecture rule for JT7 Phase 3A modular runtime extraction: core business logic must not live inside one orchestration script; Markdown defines contracts, Python modules enforce contracts, data stores reflect current state, and UI renders only verified state.
- Approved Phase 3A execution order: audit `job-search-ui/scripts/run_jt7_chain.py`, create runtime package structure, extract only the safest first modules (Actions domain, action generation, action lifecycle, Today’s Plan adapter), refactor the main script toward orchestration-only behavior, preserve current artifacts/UI compatibility, add `ACTIONS.md`, `TODAYS_PLAN.md`, and `RUNTIME_BOUNDARIES.md`, then verify no regression.
- During the first extraction pass, these new files were created under `job-search-ui/runtime/`: `domain/actions.py`, `domain/signals.py`, `domain/jobs.py`, `domain/recruiters.py`, `domain/taskruns.py`, `services/action_generation.py`, `services/action_lifecycle.py`, `services/followup_rules.py`, `adapters/homepage_state.py`, `pipelines/todays_plan.py`, `storage/local_mirror.py`, plus contract docs `ACTIONS.md`, `TODAYS_PLAN.md`, and `RUNTIME_BOUNDARIES.md`.
- Empty `__init__.py` package-marker writes failed because the write tool rejected empty content; this is non-critical and the extraction was not yet fully wired into `run_jt7_chain.py` at flush time.
- The modular extraction was in progress at compaction time; do not treat Phase 3A as complete or verified yet.

## Phase 3E / 3F modularization checkpoint
- Completed Phase 3E by extracting remaining orchestration support out of `job-search-ui/scripts/run_jt7_chain.py` into:
  - `runtime/services/review_queue.py`
  - `runtime/pipelines/chain_runner.py`
  - `runtime/pipelines/calendar_pipeline.py`
  - `runtime/pipelines/job_board_pipeline.py`
- Completed Phase 3F by further thinning the coordinator and extracting:
  - `runtime/pipelines/gmail_pipeline.py`
  - `runtime/utils/time_utils.py`
  - `runtime/utils/text_utils.py`
  - `runtime/utils/id_utils.py`
  - `runtime/utils/file_utils.py`
- `run_jt7_chain.py` is now primarily a thin entrypoint/coordinator rather than the main home of pipeline logic.
- Verified real execution after each phase with successful runs:
  - `job-search-ui/runtime/reports/jt7_run_2026-04-24T07-27-54.227609-05-00.json` (`taskrun_015`)
  - `job-search-ui/runtime/reports/jt7_run_2026-04-24T07-38-41.360224-05-00.json` (`taskrun_017`)
- Important commits from this phase:
  - `6266539` — `Phase 3E extract orchestration support modules`
  - `b709f5d` — `Phase 3F thin chain entrypoint`
- Latest known auto-sync commits produced by successful validation runs in this phase:
  - `da700ca`
  - `a15d604`
- Best next execution target after coordinator cleanup: prove Action System V1 persisted truth in live `Actions` rows/mirror artifacts before doing more refactor-driven work.
