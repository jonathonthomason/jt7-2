# 2026-05-02

## Durable session memory
- The staging writeback browser-validation phase advanced from duplicate-planning evidence to a clean browser-proven **merge** path.
- The staging UI was improved so duplicate candidates are surfaced more directly and can be merged from the staging surface instead of relying on a fragile side-panel-only action path.
- Playwright harness reliability was tightened so end-to-end staging-writeback tests run against a fresh preview build and the local runtime API instead of stale preview state.
- A focused browser proof for the merge path passed end-to-end, confirming the UI action can reach the real runtime writeback path and preserve canonical merge behavior.
- The next material issue exposed is a **create-path state-sync bug**: browser-driven canonical create writeback can succeed at runtime, but the in-app `Jobs` view does not reliably reflect the newly created canonical row after navigation during the same proof cycle.
- Recommended next engineering move after this flush: diagnose the create-path state refresh gap in `job-search-ui/src/state/mvpState.tsx`, then rerun the full staging writeback browser suite before committing further validation changes.
# 2026-05-02

## Durable session memory
- The staging writeback browser-validation phase advanced from duplicate-planning evidence to a clean browser-proven **merge** path.
- The staging UI was improved so duplicate candidates are surfaced more directly and can be merged from the staging surface instead of relying on a fragile side-panel-only action path.
- Playwright harness reliability was tightened so end-to-end staging-writeback tests run against a fresh preview build and the local runtime API instead of stale preview state.
- A focused browser proof for the merge path passed end-to-end, confirming the UI action can reach the real runtime writeback path and preserve canonical merge behavior.
- The next material issue exposed is a **create-path state-sync bug**: browser-driven canonical create writeback can succeed at runtime, but the in-app `Jobs` view does not reliably reflect the newly created canonical row after navigation during the same proof cycle.
- Recommended next engineering move after this flush: diagnose the create-path state refresh gap in `job-search-ui/src/state/mvpState.tsx`, then rerun the full staging writeback browser suite before committing further validation changes.
- The product/spec layer now explicitly defines an **application-readiness gate** across three matching dimensions: location fit, ATS optimization basis, and differentiation highlights.
- The shared flow/state model now distinguishes `Review`, `Staging`, `Mismatch Hold`, `Apply-Ready`, and `Submit-Ready`, plus a resume transformation layer driven by an optimization brief and tailored artifact re-verification.
- JobOps and JT7 Platform must match on those three application-point dimensions before a role is treated as apply-ready; if the summary and structured state diverge, the item should be held until reconciled.
- Onboarding requirements expanded to capture explicit geo/work-mode constraints, ATS keyword themes, differentiation hooks, impact themes, and screening guardrails so later application optimization is grounded in evidence rather than ad hoc interpretation.
- Highest-leverage next product move shifted from more runtime proving to defining the **canonical schema/state model** that enforces application-readiness in UI/runtime behavior.
- Jonathon explicitly asked to preserve quota: avoid broad demo-build attempts, coding-agent retries, or unnecessary task/chain runs; prefer small schema-first implementation slices and read-only state reconstruction when possible.
- A real 12:30 catch-up scheduler run fired after resume and failed on missing `gog` Sheets auth for `jonathon.thomason@gmail.com`, so unattended runtime trust is degraded again until auth is restored and minimally revalidated.
