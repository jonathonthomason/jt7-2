## 2026-04-22 pre-compaction durable memory append

- The JT7 scheduled chain now has a real transparency/reporting layer, with per-run JSON reports written under `job-search-ui/runtime/reports/` and pass logging expanded to include source checks, tracker CRUD visibility, local mirror changes, git outcome, warnings/errors, and batch assessment.
- The earlier zero-change runs were traced to two real gaps: placeholder Gmail extraction logic and malformed Sheets append behavior that treated tab-delimited strings as single-cell writes instead of row-wise values.
- `job-search-ui/scripts/run_jt7_chain.py` was upgraded from scaffolded reporting to a live Gmail-to-Sheets ingestion path for subject-line-based job alerts.
- The current live Gmail ingestion query is `label:Folders/Jobs newer_than:3d`.
- The current live parser supports structured subject-line extraction for at least LinkedIn and Indeed job-alert style emails.
- The current live write path now uses `gog sheets append ... --values-json` and `gog sheets update ... --values-json` as the correct row-wise Sheets mutation format.
- A real chain run on 2026-04-22 created live tracker rows from Gmail-derived job alerts:
  - Jobs created: 6
  - Recruiters created: 6
  - Signals created: 6
  - Actions created: 6
- Example roles/companies created in the tracker from the live Gmail ingestion run include:
  - Allstate — Senior User Experience Designer
  - Sixfold — Lead Product Designer
  - Pano AI — Senior Product Designer
  - Massimo Zanetti Beverage USA — Director of eCommerce
  - Miami Jewish Health — Senior Graphic Designer
  - JETSET Pilates® — Design & Development Manager
- The local mirror was updated for the affected tabs and committed/pushed after the successful real ingestion pass.
- Commit pushed for the live Gmail ingestion gap-fill work: `93d0848`.
- Current truth: JT7 now supports real subject-line-based Gmail job alert ingestion into `Jobs`, `Recruiters`, `Signals`, and `Actions`, but it is not yet a full deep parser for email bodies, recruiter conversations, application confirmations, rejection/state transitions, or interview reconciliation.
- The next highest-leverage runtime step is to extend the same live ingestion path to detect application/update/interview/recruiter emails and mutate existing tracker rows, not just create new cold opportunities.
