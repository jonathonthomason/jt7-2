## Memory Flush
- Session direction shifted from fragmented recovery work back toward JT7 2.0 alignment and daily operator usefulness.
- Verified state at time of flush: UI POC exists locally with Dashboard + Jobs visible; Google Sheets remains the operational source of truth; Gmail signals are available; job search wrapper works; execution friction was described as removed/unblocked; system framed as ~50% MVP.
- User clarified multiple operating frames during the session that matter going forward:
  - JT7 should be treated as a daily job command center, not just a simplified dashboard.
  - JT7-1 interaction/operating model should be fused with JT7-2 UI layer.
  - Full operating model includes Execute / Manage / Intelligence layers, with Execute primary.
  - "Today’s Plan" and action cards should return as primary surfaces rather than a passive dashboard-first model.
  - Parity means minimum functionality and UX of the attached/original JT7 conversation JSON, not just architectural similarity.
- User later reframed the relaunch as a modular job operations system with these priorities and constraints:
  - build order: stable runtime, Gmail read, Calendar read, Sheets read/write, verified proof loop, tracker schema rebuild, modular logic docs, status inference/drafts, then external source ingestion.
  - core objects: Company, Role, Application, Contact, Interaction, Event, Draft, Status, Source.
  - required statuses: Target, Applied, Recruiter Outreach, Recruiter Screen, Hiring Manager, Panel / Loop, Take Home / Exercise, Final Round, Offer, Rejected, Withdrawn, Ghosted / Stale, On Hold.
  - required tabs: Applications, Interactions, Events, Contacts, Drafts, Logic Log.
  - required modules: gmail_intake.md, calendar_sync.md, application_state_machine.md, draft_assistant.md, sheet_writer.md, manual_update_handler.md, source_router.md, audit_and_verification.md.
  - minimum proof-of-life: read one Gmail thread, match to one application, infer/confirm status, write update to Google Sheet, verify the write, log the action.
  - rules to preserve: no UI polish first; do not depend on Telegram as only reliable runtime; prefer modular documented logic over hidden chat state; every automated update must be auditable; support manual overrides; mirror logic in Google Docs and local markdown backups.
- Important state mismatch to resolve in future sessions: CURRENT.md and ops/focus.md still reflect older recovery-era assumptions, including Telegram/runtime unreliability as an active blocker, while the user later asserted execution is unblocked and the UI POC is already running locally.
